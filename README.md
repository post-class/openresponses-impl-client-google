# openresponses-impl-client-google
Python client library for Google implementing the OpenResponses specification

## Overview
This library provides an interface compliant with the OpenResponses specification for Gemini API.

## Features
- Handles streaming and non-streaming responses
- Uses type-safe Pydantic models
- Supports asynchronous processing (async/await)
